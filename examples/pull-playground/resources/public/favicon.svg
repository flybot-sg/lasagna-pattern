<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Three stacked layers — the lasagna -->
  <!-- Bottom layer: remote (gold) -->
  <rect x="3" y="20" width="26" height="5" rx="2" fill="#b59135"/>
  <!-- Middle layer: collection (teal blend) -->
  <rect x="5" y="13" width="22" height="5" rx="2" fill="#3da8cb"/>
  <!-- Top layer: pattern (accent blue) -->
  <rect x="7" y="6" width="18" height="5" rx="2" fill="#2891b8"/>
</svg>
